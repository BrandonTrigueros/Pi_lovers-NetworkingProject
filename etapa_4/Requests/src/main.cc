#include <iostream>
#include <string>
#include <map>
#include <vector>

void appendTable(std::string piece) {

}

int main(int argc, char* argv[]) {
  std::string route_table;
  std::map<std::string, std::vector<std::string>> pieces_map;

  return 0;
}